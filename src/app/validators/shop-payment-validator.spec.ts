import { ShopPaymentValidator } from './shop-payment-validator';

describe('ShopPaymentValidator', () => {
  it('should create an instance', () => {
    expect(new ShopPaymentValidator()).toBeTruthy();
  });
});
